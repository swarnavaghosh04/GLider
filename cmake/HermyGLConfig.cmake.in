@PACKAGE_INIT@

find_package(OpenGL REQUIRED)
find_package(SDL2 REQUIRED)

if(
    NOT TARGET HermyGL::HermyGL OR
    NOT TARGET HermyGL::glad
)
    include(${CMAKE_CURRENT_LIST_DIR}/MyTestLibTargets.cmake)
endif()